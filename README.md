# Fraud-Detection-System
real-time fraud detection platform that ingests a live transaction stream via Kafka, processes and scores each transaction in under 50ms using Spark + ML models, stores everything in a cloud data lake on AWS S3, and surfaces a live operations dashboard in Power BI. 
